{
  "name": "binance-funding-hedge-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Funding-aware external spot exposure hedge built with Binance Agent OS",
  "license": "MIT",
  "scripts": {
    "connect": "python3 -B agent_os_login.py",
    "start": "python3 -B server.py",
    "test": "python3 -B -m unittest discover -s tests -p 'test_*.py' -v",
    "verify": "python3 -B verify.py && node --check static/app.js"
  },
  "dependencies": {
    "@binance/binance-cli": "1.3.0"
  }
}
